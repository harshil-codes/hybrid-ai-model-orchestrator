apiVersion: v1
kind: Service
metadata:
  name: loan-backend
  namespace: loan-app
  labels:
    app: loan-backend
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: loan-backend

